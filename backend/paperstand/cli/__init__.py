"""Command line helpers that do not need a database.

``parse-report`` and ``parse-explain`` exist to make the parser's behaviour
visible: point them at a folder or a file and they say exactly what Paperstand
would catalogue, and why. They are the tools to reach for when tuning a profile.
"""
